---
trigger: always_on
---

Generate random:

cpu = random.randint(20, 80)
memory = random.randint(30, 90)
🔹 Device Status
Idle / Busy
Random toggling
🔹 Logs System

Display:

[INIT] IO Manager Loaded
[OK] Device Connected
[EXEC] Running Scheduler
